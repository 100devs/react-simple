// allows you to use react component
import React, { Component } from 'react';
import './Footer.css';

class Footer extends Component {
  render() {
    return (
        // renders footer information (keeping style in mind) 
        <footer className="Footer">
        </footer>

    );
  }
}

export default Footer;
